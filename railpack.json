{
    "build": {
        "builder": "python",
        "pythonVersion": "3.11",
        "buildCommand": "python -m pip install --user poetry && python -m poetry config virtualenvs.create false && python -m poetry install --no-dev"
    },
    "deploy": {
        "startCommand": "python -m poetry run uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    }
}