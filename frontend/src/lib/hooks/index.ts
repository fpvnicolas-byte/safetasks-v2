// =============================================================================
// HOOKS CUSTOMIZADOS - SafeTasks V2
// =============================================================================
// Arquivo central de exportação de todos os hooks customizados
// Permite imports limpos: import { useDesignTokens, useAccessibility } from '@/lib/hooks'

export { useDesignTokens } from './use-design-tokens';
export { useAccessibility } from './use-accessibility';
