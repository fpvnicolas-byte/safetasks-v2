import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Simplified config - using direct relative paths that work everywhere
  // No more alias configuration needed since we switched to ./src/lib/ paths
};

export default nextConfig;